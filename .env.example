# Get a key at https://console.anthropic.com/settings/keys
VITE_ANTHROPIC_API_KEY=sk-ant-...

# Only needed if your key is org-level rather than scoped to a workspace.
# Symptom: "This API key is not scoped to a workspace". Find the ID in the
# console URL when viewing the workspace.
# VITE_ANTHROPIC_WORKSPACE_ID=wrkspc_...
